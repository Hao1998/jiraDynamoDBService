{
  "name": "critical-issues-processor",
  "version": "2.0.0",
  "description": "Lambda function for processing Jira critical issues",
  "main": "index.js",
  "scripts": {
    "test": "echo \"No tests specified\"",
    "deploy": "serverless deploy"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.484.0",
    "@aws-sdk/lib-dynamodb": "^3.484.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": [
    "aws",
    "lambda",
    "dynamodb",
    "jira"
  ]
}